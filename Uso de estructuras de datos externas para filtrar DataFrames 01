import pandas as pd

our_series = pd.Series([10, 11, 12], index=['X', 'Y', 'T'])
df = pd.DataFrame(
    {
        'a': [2, 3, 10, 11, 12],
        'b': [5, 4, 3, 2, 1],
        'c': ['X', 'Y', 'Y', 'Y', 'Z'],
    }
)

print(df.query("c in @our_series.index"))

# resultado:
# a  b  c
0   2  5  X
1   3  4  Y
2  10  3  Y
3  11  2  Y

# Ahora puedes filtrar a partir de los valores del Series o del índice del Series.
#  Observa que el valor 'Y' aparece en la columna 'c' varias veces, por lo que cada una de esas filas se mantiene después del filtrado.

# ejercicio : Añade una declaración print() en la que puedes llamar a query() en df y
# pasarle un string de consulta que compruebe si la columna 'c' está en our_series.index.
